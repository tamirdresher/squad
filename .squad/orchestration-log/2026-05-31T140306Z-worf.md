# Orchestration Record: Worf Agent

**Timestamp:** 2026-05-31T14:03:06.842+03:00  
**Session:** State-Backend Insider Triage

## Agent Details
- **Name:** Worf
- **Role:** Safety & Reliability Gate
- **Selection Reason:** Security review and release-risk assessment required to prioritize state-backend regressions and establish gates for next insider release
- **Mode:** background

## Work Produced
- **Primary Output:** `.squad/decisions/inbox/worf-insider3-state-backend-risk.md`
- **Files:** 1 decision inbox file
- **Lines of Analysis:** ~112 lines (7 classified findings, required gates, local environment note)

## Findings Summary
- **CRITICAL (2):**
  1. **Pre-commit/post-commit hooks silently not installed for two-layer backend** (#1190, #1185) — All upgraded users with `stateBackend=two-layer` lose state writes with zero error. No `squad doctor` guard.
  2. **TEAM_ROOT dual contradictory definition in coordinator prompt** (#1163) — False Init Mode entry on worktrees without `.squad/` committed. Affects satellite repos and upgrade scenarios.

- **HIGH (3):**
  1. **`--state-backend` flag silently ignored during upgrade** (#1185) — Migration failure masked; root cause of hook gap.
  2. **`postinstall` ESM patch never reaches repo-local node_modules** (#1190) — `squad doctor` reports unfixable failures; blocks vscode-jsonrpc on global installs.
  3. **`teamRoot` written as absolute path, `config.json` duplicate key** (#1190, #1163) — Portability broken; indicates config-write structural bug.

- **MEDIUM (2):**
  1. **Template files dumped at `.squad/` root during upgrade** (#1185) — Cosmetic noise; deduplication guard missing.
  2. **Rai not installed during upgrade** (#1185) — Missing built-in roster; `.gitattributes` merge driver incomplete.

- **LOW (1):**
  1. **State documentation out of sync** (#1194) — Documentation-only; runtime catches or ignores.

## Required Gates (Priority Order)
1. **[BLOCK]** `squad doctor` must hard-fail when `stateBackend=two-layer` and commit hooks absent
2. **[BLOCK]** Patch `squad.agent.md` for single unambiguous `TEAM_ROOT` definition
3. **[BLOCK]** Fix `--state-backend` migration in `squad upgrade`; no silent no-op
4. Fix `patch-esm-imports.mjs` SEARCH_ROOTS; run ESM checks post clean install as CI step
5. Fix `teamRoot` default to `"."` and config-write merge strategy
6. Fix upgrade copy logic for template deduplication
7. Ensure Rai auto-installs during upgrade
8. Update state documentation before stable release

## Local Environment Assessment
- `squad-squad` repo uses `stateBackend: "worktree"` — not affected by two-layer hook gap
- `squad` worktree has no `stateBackend` in config (defaults), no `squad-state` branch, only `.sample` hooks
- Neither repo in corrupted state from these issues

## Status
- **Completion:** Complete
- **Recommendation:** 3 blockers must be resolved before any further insider release; 2+ have zero mitigation if `squad doctor` not improved
- **Next Steps:** Gate PR review; coordinate fixes with Data (code changes) and Seven (community communication)

---

**Co-authored-by:** Worf (Safety & Reliability Gate), Copilot
