# Data Orchestration Log — 2026-05-19T04:11:25Z

**Agent:** Data (Squad Framework Expert)  
**Directive:** Designed harness and filed `.squad/decisions/inbox/data-expanded-memory-ab-harness.md`

## Outcome

### Gated Harness Design (Gated Yes, But Don't Run Unattended Yet)

**Evidence from This Session:**
- `copilot --agent squad -p ... --share ...` returned exact sentinel
- `copilot --agent squad --resume=<session-id> -p ...` worked for second scripted turn (after parsing session id)
- PowerShell wrapper rejected `-C` and `--name` flags; cwd set externally, sessions resume by parsed id only
- Share exports contain current turn only (not complete transcript); harness must persist one bundle per turn

**Boundary:**
This proves 1–2 turn scriptability only. Full 50-turn experiment requires staged approach:
1. **3-turn dry run** (validate basics)
2. **10-turn pilot** (test full harness, capture, timeouts, contamination controls)
3. **50-turn batches** (full experiment, if pilot succeeds)

### Fallback Path

If Copilot session resume, transcript capture, timeouts, or contamination controls fail → fall back to direct Squad CLI/SDK replay (labeled substitute memory-layer evidence).

### Scaffolding Created

**Location:** `C:\Users\tamirdresher\.copilot\session-state\e9c1993c-7118-476c-acb1-9616a7fecbe1\files\expanded-memory-ab\`

**Key Files:**
- `README.md` — Overview and usage
- `RUNBOOK.md` — Step-by-step harness operation
- `config/experiment.json` — Experiment metadata
- `config/repos.json` — Repo definitions
- `config/memory-seed.json` — Memory seed templates
- `prompts/turns-50.json` — Fixed 50-turn prompt library (checksummed)
- `scripts/setup-variants.ps1` — Variant directory setup
- `scripts/run-copilot-paired.ps1` — Paired harness runner
- `analysis/extract-metrics.mjs` — Per-turn metric extraction
- `analysis/analyze-results.mjs` — Aggregate results analysis
- `schemas/transcript-row.schema.json` — Turn record schema
- `schemas/metrics-header.csv` — Metrics CSV template

### Next Steps

1. Coordinate with Worf on HB-1–HB-8 gate clearance
2. Coordinate with Picard on scope lock
3. Coordinate with Seven on protocol finalization
4. Execute 3-turn dry run (validate harness)
5. Execute 10-turn pilot (full validation)
6. If all green, proceed to 50-turn batches

## Status

Gated, ready for dry run. Awaiting Worf gate remediation evidence before pilot run.
